# 資料庫連線（由 Zeabur 或容器注入）
MYSQL_PORT=${DATABASE_PORT}
PASSWORD=lZAUgN25x80t6B9J7D1L43fPrIYqdRnC
MYSQL_ROOT_PASSWORD=${PASSWORD}
MYSQL_USERNAME=root
MYSQL_DATABASE=zeabur
MYSQL_HOST=${CONTAINER_HOSTNAME}
MYSQL_PASSWORD=${MYSQL_ROOT_PASSWORD}

# Prisma / 應用程式使用的主連線字串
DATABASE_URL="mysql://${MYSQL_USERNAME}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}"

# 應用層設定
APP_BASE_URL="https://your-domain.com"  # 本機可為 http://localhost:3000
SESSION_SECRET="請放長字串隨機密鑰"

# 物件儲存（預留，MVP 先不使用）
S3_ENDPOINT=""
S3_REGION=""
S3_BUCKET=""
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
